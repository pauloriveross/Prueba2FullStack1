spring:
  application:
    name: cliente

  datasource:
    url: jdbc:mysql://localhost:3306/db_cliente
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver

  flyway:
    enabled: true
    repair: true

  jpa:
    hibernate:
      ddl-auto: none